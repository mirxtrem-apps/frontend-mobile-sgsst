import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.white;
  static const accent = Color(0xFFFF873C);
  static const primaryLight = Color(0xFFFEA832);
  static const secondary = Color(0xFFFCBF29);
  static const secondaryLight = Color(0xFFFEDB41);
  // Font Colors
  static const black = Colors.black87;
  static const grey = Color(0xFF555A66);
  static const greyLight = Color(0xFF707789);
}